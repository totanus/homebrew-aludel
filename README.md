# homebrew-aludel

Homebrew tap for [aludel-cli](https://aludel.matteobalocco.it/cli.html), the
command-line version of Aludel.

```bash
brew tap totanus/aludel
brew install aludel
```

macOS, Apple Silicon only. Bundles Piper + ffmpeg (see the formula) — no
separate install needed.
